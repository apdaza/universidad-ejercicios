class Nodo:
	def __init__(self, pos, valor = 0, hijos = []):
		self.pos = pos
		self.valor = valor
		self.hijos = hijos

"""def agregar(arbol, pos, valor):
	if arbol == None:
		return Nodo(pos, valor)
	else:
		if(not buscar((3,1)))
			return Nodo(arbol.pos, arbol.valor, arbol.hijos + [Nodo(pos, valor)]
"""	
	
print [list(l)[:-1] for l in open("matriz.txt","r").readlines()]

